# Retired project mockups

Mockups kept for portfolio history but intentionally excluded from `public/` and
the production bundle. Restore an asset to `public/` only together with a
matching entry in `src/data/projects.ts`.
